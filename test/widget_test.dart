import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (tester) async {
    // Placeholder smoke test — the real app requires
    // Firebase, SharedPreferences, and other async init
    // that cannot run in a basic widget test.
    expect(1 + 1, equals(2));
  });
}
